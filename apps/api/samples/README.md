# Sample design files (API-served)

| file | use |
|------|-----|
| `design.sample.xlsx` | word / term / domain |
| `code-design.sample.xlsx` | code |

Served by `GET /v1/chk-db-std/samples` and `GET /v1/chk-db-std/samples/{id}`.
